export default function Home() {
  // Esta página no se usará, pero Next.js la requiere para construir la app
  return (
    <main style={{ padding: '2rem' }}>
      <h1>API Backend Running</h1>
      <p>Usa los endpoints /api/...</p>
    </main>
  )
}